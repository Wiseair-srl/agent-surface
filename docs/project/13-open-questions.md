# 13 — Decision Log and Open Questions

> [!NOTE]
> Part A records every decision this spec was required to make explicitly: the chosen v0.1 behavior, the alternatives considered, the trade-off, and whether the choice is considered settled (**S**) or revisitable with evidence (**R**). Full normative text lives at the linked sections — this table is the index, not a second source of truth. Part B lists what is genuinely open.

## Part A — Decision log

| # | Question | Decision (v0.1) | Alternatives considered | Trade-off accepted | S/R | Spec |
|---|---|---|---|---|---|---|
| D1 | How is `surfaceVersion` generated/updated? | Per-registry monotonic counter (opaque string) + random `surfaceId` per page load; bumps on structural + *pushed* availability changes only; lazy `when()` drift doesn't bump | Content hash (expensive, no ordering); timestamp (collisions, clock skew); bump-on-everything (version storms) | Version ≠ full freshness: `when` is authoritative at invoke, not in the number | S | [Core API §versioning](../03-core-api.md#versioning) |
| D2 | Can a registration change definition without deregistering? | No: structure frozen per registration; only `enabled`/availability/handlers are dynamic; structural change ⇒ re-register (new `registrationId`) | Mutable definitions + change events (staleness tokens become meaningless); versioned definitions per registration (complexity) | Occasional re-register churn in exchange for "reg_x always means the same thing" | S | [Core API §lifecycle](../03-core-api.md#registration-lifecycle) |
| D3 | React handlers changing every render? | Latest-ref pattern: handlers read at invocation time; no deps array, no re-registration, no version bump | Deps-array re-registration (churn + stale closures); forced `useCallback` (footgun) | Handlers are impossible to memo-freeze — deliberate | S | [React API §handler-freshness](../04-react-api.md#handler-freshness-d3--why-there-is-no-dependency-array) |
| D4 | How are ID collisions detected? | Key `(plane,type,instanceId)`; structural dupes throw; runtime dupes → dead handle, first-wins (opt-in `"replace"`); cross-plane suffix heuristic warns | Throw always (crashes prod on transition races); last-wins (silent hijack) | Transient legit collisions (crossfades) need `enabled:false` or `"replace"` | S | [Core API §collisions](../03-core-api.md#collisions-d4) |
| D5 | Observations on demand or embedded in snapshot? | On demand only; snapshot is a pure sync catalog | Eager embedding (token waste, side-effect risk, forces async snapshot); opt-in embedded previews | Extra round-trip per read; revisit via OQ-8 hints | S | [Core API §snapshot](../03-core-api.md#snapshot) |
| D6 | How is surface size limited? | Hard caps on descriptions/meta/schemas; flat snapshot + `priority`; opt-in budget with explicit `truncated` marker (Experimental) | Hierarchical/query-navigable surface; automatic relevance ranking | Simple + predictable now; smart filtering deferred (OQ-4) | R | [Core API §limits](../03-core-api.md#limits-and-defaults), [§snapshot](../03-core-api.md#snapshot) |
| D7 | Representation of partially bound inputs? | Agent-facing schema = original minus locked bound fields; `boundFields` metadata; values never in snapshots | Full schema + docs-only convention (models fill bound fields anyway); separate "context args" channel | Schema surgery must be correct (tested); top-level fields only (OQ-6) | S | [oRPC integration §binding](../05-orpc-integration.md#binding-semantics-d7-d8--draft) |
| D8 | Can the agent override bound values? | Locked by default ⇒ `INVALID_INPUT`; explicit `overridableFields` opt-out (value becomes a default) | Overridable by default (reintroduces model guessing of authoritative UI state) | Some legit override flows need explicit opt-in | S | [oRPC integration §binding](../05-orpc-integration.md#binding-semantics-d7-d8--draft) |
| D9 | Agent-visible vs internal metadata? | Two fields: `meta` (JsonValue, capped, serialized) vs `internal` (never serialized; policy/audit-only). Non-serialization is a tested invariant | Single bag + serializer allowlist (easy to leak by omission) | Slight duplication when a value is needed in both | S | [Core API §definitions](../03-core-api.md#definitions), [Policies & Security §minimization](../06-policies-and-security.md#data-minimization-checklist-authoring-norms) |
| D10 | Which policies client-side vs re-run server-side? | Client = UX + honesty (visibility, availability, confirmation UX, advisory rate/authz); server re-checks authn/authz/tenant/input/rate/approval on every domain call | Trusting client evaluation for "low-risk" ops (no such thing) | Double evaluation cost, by design | S | [oRPC integration §D10 table](../05-orpc-integration.md#what-the-client-checks-vs-what-the-server-must-re-check-d10) |
| D11 | Representing temporarily disabled capabilities? | In snapshot with `available:false` + `unavailableReason`; invoking ⇒ `CAPABILITY_NOT_AVAILABLE {reason}` | Hide when disabled (agent can't plan the enabling step) | Slightly larger snapshots | S | [Concepts §availability](../01-concepts.md#availability-visibility-and-the-deny-by-default-rule) |
| D12 | Hide unauthorized capabilities or show as unavailable? | **Authority hides, state discloses**: authz failures hidden (and `CAPABILITY_NOT_FOUND` on invoke); state failures visible-disabled | Show-all-with-flags (leaks existence); hide-all (starves agent planning) | Policy authors must pick the right decision type | S | [Policies & Security §hide-vs-disable](../06-policies-and-security.md#hide-vs-disable-d11d12-restated-as-the-policy-authors-rule) |
| D13 | Concurrent invocations? | Actions serialized FIFO per component instance, queue depth 2, overflow `RATE_LIMITED{queue-full}`; observations **bounded** (per-consumer 8 / global 32 / queue 8 — amended by D24; "unlimited" was a P0 bug) | Per-capability serialization (allows intra-instance races); global lock (needless coupling); unlimited (unbounded load/memory) | Cross-instance races possible (rare; accepted) | R | [Core API §concurrency](../03-core-api.md#concurrency-timeouts-cancellation) |
| D14 | Preventing double executions? | **Corrected by D22:** key = `(consumerKey, invocationId)` per registry + request fingerprint; join/cached only on fingerprint match; mismatch fails `INVOCATION_CONFLICT`; in-flight joining + terminal-result cache (excl. `CONFIRMATION_REQUIRED`, `RATE_LIMITED`, `INVOCATION_CONFLICT`); adapters reuse provider tool-call ids *within their consumer namespace* | Bare-`invocationId` cache (provider ids collide across consumers/pages → cross-request replay, the P0 bug); no dedupe; handler-level idempotency only | Bounded cache = dedupe window, not a guarantee forever | S | [Core API §identity](../03-core-api.md#invocation-identity-idempotency-conflict-safety-d14-as-corrected-by-d22) |
| D15 | Timeouts and cancellation? | Per-kind defaults (5/10/30 s), per-capability override; cooperative `AbortSignal`; `TIMEOUT` cached terminal (retry needs new id); external abort ⇒ `CANCELLED` | No timeouts (hangs); hard kill (impossible in JS) | Non-cooperative handlers can still leak work (logged as late-settlement) | S | [Core API §concurrency](../03-core-api.md#concurrency-timeouts-cancellation) |
| D16 | Navigation unmounts component mid-execution? | **Amended by D23:** non-navigation actions keep first-settle-wins (`ok` or `COMPONENT_UNMOUNTED{mid-flight}`, loser logged); `navigation`-effect actions settle on **handler settlement only** — unmount never overwrites a committed transition | Timing-dependent classification for navigation (the P0 bug: success reported as `COMPONENT_UNMOUNTED`); block unmount until settle (fights React); silent zombie handlers | Agent must treat `mid-flight` as "verify state" | S | [Core API §concurrency](../03-core-api.md#concurrency-timeouts-cancellation), [Errors §COMPONENT_UNMOUNTED](../07-errors.md#component_unmounted) |
| D17 | Event-ordering guarantees? | Total mutation order; post-mutation dispatch; coalesced `surface-changed`; queued (non-re-entrant) nested mutations; listener exceptions isolated; started<settled pairs | Fully async event bus (loses ordering); sync re-entrancy (corruption risk) | Listeners see slightly deferred nested mutations | S | [Architecture §guarantees](../02-architecture.md#concurrency-and-ordering-guarantees) |
| D18 | Input/output serialization? | `JsonValue` only; ISO dates; undefined stripped; dev-mode round-trip probe; output size cap fails loudly (`output-too-large`) | structuredClone semantics (not portable to wire); binary support (deferred) | No Dates/Maps/binary in payloads for now | S | [Core API §serialization](../03-core-api.md#serialization-rules-d18-draft) |
| D19 | JSON Schema subset? | Closed allowlist (2020-12 core + anyOf + internal $defs; no if/then, oneOf, allOf, patternProperties…); validated at registration; depth/size caps | Full JSON Schema (LLM + validator burden); provider-specific subsets (fragmentation) | Some Zod features won't convert; fails fast at registration | R | [Core API §subset](../03-core-api.md#supported-json-schema-subset-d19-draft) |
| D20 | Zod integration without hard dependency? | `AgentSchema` interface; `fromStandardSchema(schema,{jsonSchema})` (validation via Standard Schema, conversion supplied by caller — Zod 4's `z.toJSONSchema` is one line); `fromJsonSchema` + minimal built-in validator as floor | Hard Zod dep in core (violates independence); auto-detect converters (magic, version-fragile) | One extra line per schema for Zod users (see the `zs` helper in [Examples](../10-examples.md#schemas-host-app)) | S | [Core API §schemas](../03-core-api.md#schemas) |
| D-eff | Do `server-query`/`server-mutation` belong to frontend actions? | No: server effects reserved to procedure references; view actions = `local-state`\|`navigation` (enforced, `PLANE_VIOLATION`); indirect refetches remain `local-state` | Allow server effects on view actions (recreates shadow-RPC, kills the plane distinction) | Apps must define procedures for direct server work — that's the point | S | [Concepts §effects](../01-concepts.md#effect-taxonomy) |
| D21 | Can input-aware policy/confirmation run before validated effective input exists? | No — P0 correction: two-stage policy API (`onAuthorize` pre-input, `onInvoke` post-input with `effectiveInput` only); confirmation decided at phase 6, evidence bound to canonical request digest {surfaceId, registrationId, capabilityId, consumerKey, effectiveInput, effect}; canonical JSON defined (sorted keys, `-0`→`0`, finite numbers, `undefined` omitted) | Single `onInvoke` with optional raw `input` (the contradiction: confirmation over unvalidated/unbound input); convention-only ordering (type system must forbid it) | Breaking Draft-API change; two onion chains instead of one | S | [Spec Corrections RFC §c1](18-spec-corrections-rfc.md), [Architecture §pipeline](../02-architecture.md#invocation-pipeline-normative-order), [Policies & Security §policy-pipeline](../06-policies-and-security.md#policy-pipeline) |
| D22 | Invocation identity across consumers/adapters/pages? | Key `(consumerKey, invocationId)` per registry; fingerprint = fnv1a64(canonical request **as issued**); same key+different fingerprint → `INVOCATION_CONFLICT` fail-closed; anonymous → `embedded:anonymous`; adapters own per-instance consumer ids | Bare `invocationId` (collisions replay foreign results); fingerprint over *effective* input (impossible at phase 1 — recorded deviation from directive sketch, effective input is confirmation's digest instead); global registry-crossing cache (page reloads must isolate) | Conflict error surfaces adapter id-bugs loudly | S | [Spec Corrections RFC §c2](18-spec-corrections-rfc.md), [Core API §identity](../03-core-api.md#invocation-identity-idempotency-conflict-safety-d14-as-corrected-by-d22), [Errors §INVOCATION_CONFLICT](../07-errors.md#invocation_conflict) |
| D23 | What defines navigation success under owner unmount? | Handler-settlement authority for `effect:"navigation"`: unregistration never settles the invocation; resolve→`ok`, reject+aborted-signal→`CANCELLED`, reject→`EXECUTION_FAILED`, else timeout/cancel; handler resolves on router acceptance | Injected `AgentNavigationHost` seam (second place to express one contract — designated fallback if real routers can't report acceptance in-handler); first-settle-wins (timing-dependent, the P0 bug) | Navigation handlers must settle on acceptance (sync routers trivially do) | S | [Spec Corrections RFC §c3](18-spec-corrections-rfc.md), [Concepts §effects](../01-concepts.md#effect-taxonomy), [Core API §concurrency](../03-core-api.md#concurrency-timeouts-cancellation) |
| D24 | Are all runtime collections bounded? | Yes — new limits: observations per-consumer 8 / total 32 / queue 8 (overflow `RATE_LIMITED{queue-full}`, never the action queue); `maxPendingConfirmations` 32 (overflow fails closed, no record); defaults conservative until example-app measurements | Unlimited observations (P0: avoidable load/memory); expiring oldest pending confirmation (silently invalidates an approval being read) | Hard limits may need tuning with real data (revisitable defaults, not shape) | S | [Spec Corrections RFC §c4](18-spec-corrections-rfc.md), [Core API §concurrency](../03-core-api.md#concurrency-timeouts-cancellation), [Core API §limits](../03-core-api.md#limits-and-defaults) |
| D25 | Configurable action concurrency? | Implemented as specified: `instance` (default) \| `capability` \| `key` \| `parallel{max}`, per-group `queueDepth`, bounded `max` enforced at definition time; procedure refs default to one group per procedure identity per registration; groups dropped when idle; not model-visible | Implement at spec time (directive §3.5 forbade it before base-queue races were green); never specify (adapters invent shapes); unbounded `parallel` (the one place the runtime would stop being bounded) | Procedure calls that previously ran unbounded-parallel now serialize per identity — safer, and a behavior change for anyone who relied on the old fan-out | S | [Spec Corrections RFC §c5](18-spec-corrections-rfc.md), [Core API §concurrency](../03-core-api.md#concurrency-timeouts-cancellation) |
| D26 | Default confirmation mode? | By declared topology: embedded→`wait`, remote→`two-phase`; `createAgentToolset` throws if neither `topology` nor `confirmations` given; WebMCP fixed two-phase; dispose deterministically settles pending waits | Global `wait` default (P0: holds remote runs open across human approvals); global `two-phase` (punishes the embedded majority case) | One more required option at toolset creation | S | [Spec Corrections RFC §c6](18-spec-corrections-rfc.md), [Adapters §confirmation-topology](../09-adapters.md#confirmation-topology), [Core API §toolset](../03-core-api.md#toolset) |

| D27 | Can a model-supplied `surface_discover({scope})` change what it may discover? | Only narrower: the adapter-configured `scope` is a **floor**, intersected pairwise with the request; `[]` means "unspecified", not "everything"; a disjoint request returns an empty surface, never a fallback to the floor. Meta mode otherwise resolves targets exactly as direct mode (`AS-ADAPTER-004`), and `budget` is meta-only | Model scope replaces the configured one (the shipped 0.1 behavior — a scoped adapter could be widened back by asking); ignore the parameter entirely (loses the real use case, lazy narrowing on a large surface) | Scope stays a *discovery* filter in both modes — `invoke` never checks it, so a least-trusted peer belongs on `direct`, where the floor removes the tool rather than hiding it | S | [Adapters §meta-tools-mode](../09-adapters.md#meta-tools-mode), [Adapters §scope-is-discovery-only](../09-adapters.md#scope-is-discovery-only) |

| D28 | Where does volatile capability state live — in the description, or beside it? | Beside it, at **both** layers: `AgentTool.state {available, unavailableReason?, note?}` and `AgentProcedureDescriptor.contextualNote`, so a provider tool block is byte-stable across steps and the host renders state where it can afford to invalidate. Compatibility flags `AgentToolsetOptions.descriptionIncludesState` and `RegistryOptions.snapshotMergesContextualNote` shipped in 0.3 defaulting to 0.1's behavior and were **removed outright in 0.5** — never flipped, since pre-1.0 they were carrying a migration nobody had asked for (the planned one-minor window had already run to three). The split is now the only composition; `stableDescriptionOf` went with them, having existed only to paper over the two modes. `describePrefix` keeps plane/effect/confirmation — those are properties of the capability, not of the moment | Keep composing into the description (forecloses prompt-prefix caching for every host, at any catalog size — the tool block is the cache prefix and `available` flips whenever the user clicks); host-side parsing of the prefix (brittle, and the binding note is unrecoverable); drop the availability text (D12's "state discloses" is real planning fuel) | The `[currently unavailable: …]` signal must be re-rendered by the host or it is lost from the model's view — a real migration burden, which the flags deferred and 0.5 stopped deferring | S | [Catalog Scale RFC §c1](19-catalog-scale-rfc.md), [Adapters §rendering-capability-state](../09-adapters.md#rendering-capability-state), [Core API §toolset](../03-core-api.md#toolset) |
| D29 | Is `mode:"meta"` Experimental or supported? | **Experimental** — graduated to Supported in 0.2 on a conformance suite pinning its distinguishing behaviors (`AS-META-001…005`), and **returned to Experimental in 0.7**. Graduating it forced one addition — `surface_act` takes `surfaceVersion`, because a direct tool carries the version of the catalog it was built from and the meta verb re-resolves per call, so without an echoed token the staleness guard for destructive effects could never fire. What the graduation suite did *not* cover was the verbs' own envelope, and that is where 0.6 found two defects against a live model (D32, `AS-META-007/008`): an untyped `input` the provider's decoder could not constrain, and `required`/`additionalProperties` declared but never enforced. Both made the mode materially less reliable than `direct` for the same capabilities; neither was reachable through the five behaviors the suite pinned. Pinning what a mode does *differently* is not evidence that its contract has settled | Keep it Supported and absorb envelope changes as patches (the label would then mean "we found two protocol defects here in one minor and call it stable anyway"); auto-switch `direct→meta` above N (OQ-9: measure first) | The cost the 0.2 graduation was meant to remove comes back: this is still the library's only answer for a catalog that cannot fit a provider tool block, and the marker discourages adoption exactly there. Mitigated, not denied — the conformance suite still gates every pinned behavior, and 09 tells hosts to read the marker as "expect the envelope to move" rather than "unfinished". Unchanged either way: one extra round trip before the first act, and `subscribe` never fires | S | [Catalog Scale RFC §c2](19-catalog-scale-rfc.md), [Adapters §meta-tools-mode](../09-adapters.md#meta-tools-mode) |
| D30 | What stops a wire name from breaking the provider limit or losing its identity? | Enforcement plus an authoritative map: names are always ≤ 64 chars (already true in 0.1 — the RFC's premise that they were not is corrected here), shortened with a `_0_` marker, collision-checked **across the emitted catalog** (`assignWireNames`), and reversed through `toolset.wireNameMap()`. `decodeWireName` now returns `undefined` for anything it cannot re-encode byte-identically, including `_at_<instance>` names — it previously answered `view:devices.table.readState_at_main`, a plausible id that is not the capability's | Leave decoding to string surgery (silently wrong canonical ids: the id *is* the audit identity); per-name uniqueness only (collisions are a catalog property); longer fixed hash (costs budget in every name to fix a case that needs escalation in a few) | Emitted names change format for capabilities whose encoding exceeded 64 characters — those were undecodable before and are now explicitly so | S | [Catalog Scale RFC §c3](19-catalog-scale-rfc.md), [Adapters §wire-names](../09-adapters.md#wire-names) |
| D31 | Is a scope floor's refusal visible to the model it affects? | Yes: `surface_discover` sets `scopeRejected: {prefixes}` on the requested prefixes the floor admitted nothing for — the whole request when disjoint, a subset when partial. The marker is **adapter-produced**; `snapshot()` never sets it, having no floor to intersect against. A prefix broader than the floor is not a refusal (it collapses to the floor's prefix — D27's narrowing). The three meta verbs also describe their parameters, `scope` by description rather than `enum` (live component types in a tool schema would churn the block that `AS-META-005`/D28 keep byte-stable) | Leave it unmarked (the shipped 0.2 behavior — a blank payload reads as "the surface is empty", which is the one conclusion the model must not draw, and the opposite of what `AS-META-003` marks truncation for); move scope intersection into the registry so one producer owns narrowing and marker both (cleaner ownership, but the floor is adapter config and the registry has no concept to intersect against) | Two producers write one versioned payload — the registry sets `truncated`, the adapter sets `scopeRejected`. Accepted because the alternative pushes an adapter concept into core to fix a naming problem, not a correctness one | S | [Adapters §meta-tools-mode](../09-adapters.md#meta-tools-mode), [Core API §snapshot](../03-core-api.md#snapshot) |
| D32 | Who validates a meta verb's own arguments, and what happens to an `input` that arrives as a JSON-encoded string? | The adapter, before the registry sees the call: each verb is checked against its **own** declared schema (`required`, declared scalar types, `additionalProperties:false`) and returns `INVALID_INPUT` naming the offending property — the unknown-key message says the argument belongs inside `input`. `surface_act.input` is declared `type:"object"` (it was the only untyped property in the block, so constrained decoding had nothing to constrain there), and a stringified `input` is parsed **only** when the resolved target's own schema declares an object, with a dev warning. `parseCapabilityId` now rejects non-strings rather than throwing | Leave enforcement to the provider (the shipped 0.5 behavior: a missing `capabilityId` reached `parseCapabilityId` as `undefined`, and the caller's own mistake came back as `EXECUTION_FAILED{handler-error, retry:"no"}` — an internal-defect report carrying the one retry hint that says "stop"); reject a stringified `input` instead of parsing it (correct and useless — nothing tells the model which of the two malformations it made); parse it for any target (would take a capability's declared string input apart under it) | Two validators for one call, and a repair the model is never told about. Accepted: the envelope and the capability input are different objects with different owners, and the warning keeps the repair visible to the host rather than to the model | S | [Adapters §meta-tools-mode](../09-adapters.md#meta-tools-mode) |
| D33 | How does a developer find out *why* a capability is missing, without re-leaking what `hide` withholds? | A second projection, `explainSurface()`, behind its own entry point (`@agent-surface/core/explain`). It reports every capability the registry holds — hidden ones included — with each policy in the chain, its scope, its own discovery vote, and whether its `onDiscovery` threw; `availability` stays separate from the votes so "a policy removed it" never looks like "the UI says not right now". Reachability is the enforcement: `AS-EXPLAIN-004` fails the build if it appears on the package root adapters import, and `AS-EXPLAIN-003` pins its composed outcome to the snapshot's for the same context | Add a debug flag to `snapshot()` (one payload with two audiences and a boolean deciding which — the failure mode is a host forwarding the debug shape to a model); gate it at runtime on `environment !== "production"` (implies an agent-facing leak is fine in development, and blocks inspecting a production-configured registry from a terminal); name only the layer that hid a capability, not the policy (smaller disclosure, but bisecting by layer is the work it was meant to remove) | A developer-only view of exactly what the threat model withholds, kept honest by import-reachability rather than by a runtime condition. Discovery policies also run a second time — safe by contract (sync, cheap, side-effect free), but it is a contract explain now depends on | S | [Core API §explainSurface](../03-core-api.md#explainsurface--developer-projection), [Policies & Security §explain is never agent-facing](../06-policies-and-security.md#explain-is-never-agent-facing), [CLI](../20-cli.md) |
| D34 | What does a tool that *hosts* someone else's application owe the process it runs in? | Two guarantees, both the host's to keep. **stdout is the command's output and nothing else** (`AS-CLI-004`): core's development audit sink writes to stderr, because under Node `console.debug` is `console.log` and a registry built the ordinary Vite way (`import.meta.env.PROD ? … : "development"`, and `PROD` is `false` under vite-node) put a registration trace on the stream `inspect --json` renders into. **The command ends when it is done** (`AS-CLI-005`): an unref'd grace timer lets a finished run exit naturally, and its firing *is* the diagnosis that the run was wedged — so the CLI names the handles still open and exits anyway | For the stream: `console.error` in the browser too (turns a dev trace red in devtools); leave it and document that configs must not use `environment: "development"` (moves a library defect onto every consumer, and consumer-side console filtering is fragile by construction). For the exit: bare `process.exit()` (masks the leak entirely — the user learns nothing); read the handle table eagerly instead of after a grace (blames every healthy run for the CLI's own vite socket, still closing at that instant) | The audit trace changes stream under Node, so anything grepping stdout for it must move to stderr. A wedged run now costs one extra second before it is force-exited, and a genuinely app-owned pipe would be filtered out of the report along with the CLI's own stdio | S | [CLI §output modes](../20-cli.md#output-modes), [CLI §exiting](../20-cli.md#exiting), [Policies & Security §audit](../06-policies-and-security.md#audit) |

| D35 | Can the capability *catalog* be read without mounting the app, and does reading it create an exposure path? | Yes, and no. `agent-surface inspect --depth static` builds a static catalog (`agent-surface capabilities` until D38 merged it) from the TypeScript program over the project's own `tsconfig` — no Vite server, no jsdom, no scenarios, no mount. Identity is recovered from source text (`type` is a string literal, capability names are object keys), which is all a capability id is made of; `instanceId` is dynamic and is not part of one. Each entry carries `resolution: static | partial | unresolved`, and an unreadable call site is emitted **with its file, line and the construct that defeated the extractor** (`AS-COVER-002`) — never dropped. `check` exits non-zero when any call site is unread unless `--allow-unresolved` is passed (`AS-COVER-003`; the exit moved from `capabilities` to the gate in D38). It reads code and creates nothing: directive §2.1 is untouched, and `AS-COVER-006` pins the inventory out of the package root adapters import, mirroring `AS-EXPLAIN-004`. The `domain:` plane is reported `not analyzed` rather than as zero (OQ-1). Extraction follows **one hop** to a same-module `const`, then reports `unresolved` | Vite module-graph traversal from the app entry (tighter than tsconfig globs, but reintroduces the boot this exists to avoid); a Babel/AST pass (no import resolution, so it degrades where the type-checker would not); infer the catalog by mounting every route (that is the thing being checked); drop unreadable call sites silently (the failure this decision exists to remove — a partial understanding reporting itself as complete) | The inventory is an **upper bound**: tsconfig include globs are wider than what a bundle reaches, so a capability in a component no route renders any more is in it. That is dead code, a different finding, not a false positive — and the summary line says `upper bound`. Files outside the config's directory are skipped and counted, because a workspace tsconfig aliases the library's own source into the program, where `registry.register(definition)` inside `useAgentComponent` reads as an unresolvable registration | S | [CLI §the catalog](../20-cli.md#the-catalog---depth-static), [Surface Coverage RFC §c1](21-surface-coverage-rfc.md) |
| D36 | Which authored capability does no scenario reach, and does a policy-hidden one count as reached? | The verdict joins the D35 catalog (`agent-surface coverage` until D38 folded it into every command) against the union of every scenario's **explanation**, on `capabilityId` (instance-independent by construction), and reports `unreached` / `undeclared` / `unresolved`. A policy-hidden capability is classified **reached** (`AS-COVER-004`): a scenario mounted it and the policy made a deliberate decision about it. Adoption ratchets rather than gates — a committed `.agent-surface/coverage-allow.json` of `{capabilityId: reason}` suppresses known gaps, and an entry that is *no longer* unreached fails the command so the list cannot silently rot (`AS-COVER-005`). Exit codes follow `AS-CLI-002`. `undeclared` reports without failing (OQ-14); `domain:` ids are held apart from it, since the inventory never claimed that plane | Join on the snapshot instead of the explanation (the example app's `anonymous` scenario alone would contribute eleven false gaps — the library's own correct behavior reported as defects); gate CI immediately with no allowlist (a check adoptable only big-bang is one nobody adopts); per-component allowlist entries (one line for a whole unmounted screen, but it hides a newly added capability inside an already-allowed component — the second failure mode is the one this is for); fail on `undeclared` (a dynamic registration is legitimate and, from outside, indistinguishable from an extractor gap) | Coverage is measured against the inventory's upper bound, so a dead component reads as a gap until someone deletes it — deliberately, since the alternative is not noticing. A capability that is neither authored statically nor reached at runtime is still invisible: **a UI affordance that was never registered is out of reach of everything here**, and human diff review remains the only gate | S | [CLI §the verdict](../20-cli.md#the-verdict), [Surface Coverage RFC §c2](21-surface-coverage-rfc.md) |
| D37 | What may a CLI assert about completeness, given it can only see what a scenario mounted? | Only what it can back. Four corrections, all to what the commands *say*. **Rejections are reported** (`AS-CLI-006`): a duplicate `(type, instanceId)` or an `onRegister` rejection yields a dead handle that reaches neither the snapshot nor the explanation nor any baseline, and whose only diagnostic goes through `devError` — which prints nothing under the `environment: "test"` the documented config shape builds; the collector now reads `component-rejected` off the event stream the harness already records. **Counts name what they are relative to** (`AS-CLI-007`): the scenario always, and the scope when one is active, since a scope filters snapshot *and* explanation. **`hidden` prints unconditionally** — it was already computed on every run, and suppressing it outside `--explain` rendered a policy-emptied surface as `0 callable, 0 visible-disabled` under the words *nothing is registered*. **`check` names the scenarios it compared**, because a green `check` means "did not change *in these scenarios*" | Leave the rejection to `devError` and tell configs to build `environment: "development"` (moves a library defect onto every consumer, and floods stdout with the audit trail D34 just moved off it); fail `check` on a rejection (it is a mount-time defect, not surface drift — different question, different command); print `hidden` only under `--explain` as before (the status quo, which is the misleading check); keep the count-only success line (`2 scenarios` reads as a statement about the surface) | More output on every run, including runs where nothing is wrong: one extra count on the header, one extra line under a green `check`. Accepted — the guiding rule is *better a missing check than a misleading check*, and each of these removed a claim the CLI was making and could not back | S | [CLI §inspect](../20-cli.md#inspect), [CLI §check](../20-cli.md#check), [Surface Coverage RFC §c3](21-surface-coverage-rfc.md) |
| D38 | Should the catalog and the projection be separate commands, and what must a green `check` be allowed to mean? | No, and less than it did. D35–D37 recovered the catalog by adding two commands, which cut the surface along an **implementation seam** — *does this boot a TypeScript program? does it need jsdom?* — rather than along a question anyone has. The cost was not aesthetic: `check` gated on drift alone and printed a line saying that capabilities no scenario mounts were `coverage`'s question, so in CI an unreached route sat behind a green tick while the tool that knew said nothing. A gate that names the check it is *not* performing is a gate with a hole in it. So: **one command per question** — `init` / `inspect` / `snapshot` / `check`, matching `orpc-agent`'s surface — with **`--depth static\|runtime\|full`** selecting which halves are computed (`AS-CLI-008`), `full` by default because a tool that has to be asked for the complete answer mostly gives the incomplete one. The gap reaches all three mounting commands (`AS-COVER-007`); **`check` alone fails on it**, and `inspect` never exits non-zero on a finding, because a viewer that sometimes fails is a viewer nobody pipes. `capabilities`/`coverage` removed, not aliased — naming either prints where its answer went. Exit `2` widened from *usage error* to *could not run*, `orpc-agent`'s meaning | Keep five commands and make `check` shell out to `coverage` (same hole, one more moving part); make `inspect` fail on findings too (a viewer in a pipeline has to be safe to run); add `--fail-on drift\|coverage\|all` (a second, coarser way to say what `coverage-allow.json` already says per-capability — the fine-grained dial is the one that shrinks); alias the removed commands for a release (pre-1.0, and an alias that silently answers a *different* question than it used to is worse than an error naming the replacement) | Every mounting command now pays one TypeScript program boot by default — under a second on the example app, more on a wide tsconfig, which is what `--depth runtime` is for. `check` gained three failure classes, so a repository adopting it needs the allowlist from day one. `inspect --json` changed shape: the per-scenario array moved under `scenarios`, beside `catalog`, `coverage` and `failures`, each `null` when the depth did not compute it | S | [CLI §two sources of truth](../20-cli.md#two-sources-of-truth), [CLI §depth](../20-cli.md#depth), [Surface Coverage RFC §amendment](21-surface-coverage-rfc.md#amendment--the-command-cut-was-wrong-d38) |
| D39 | Which command gets a terminal UI, and who decides? | Every command, and one presenter — never the command. The renderer choice was made at each call site, so three of them were never made: `check` and `snapshot` drew no terminal UI at all (a `check` on a real repository was seventeen silent seconds and then a wall of plain text), `inspect` printed its static catalog and its mount failures as raw text in the middle of a drawn report, and one report carried two text columns because each block sized its own label field. So the commands stopped rendering: they build a **report model** — blocks, tables, findings, notes, next steps — and a presenter draws it (`AS-CLI-014`), choosing Ink or plain text **per stream** so a drawn run whose stderr is redirected still writes a readable file, owning the blank line between parts, and holding one label grid for the whole report. `check` stays plain wherever it is actually read — piped, `CI`, `NO_COLOR` (`AS-CLI-003`) — which was the real content of "`check` is always plain"; what it lost was the *branch*, not the guarantee | Leave `check` plain by construction (the status quo: a documented rule that only held because the command had no renderer, and that said nothing about the seventeen seconds); make `inspect` plain too, levelling down (throws away the one command whose output people actually watch); a shared style guide rather than a shared presenter (a convention every new call site is free to miss, which is exactly how this drifted) | The label grid widened from 12 to 14 columns, so `check`'s matrix rows moved right by two — human output only, no committed artifact changes. `snapshot` gained a run header and prints the files it wrote as a table rather than as `wrote …` lines. A terminal now sees colour and a spinner from `check` and `snapshot`, which CI never will | S | [CLI §one report, one look](../20-cli.md#one-report-one-look), [CLI §output modes](../20-cli.md#output-modes) |
| D40 | What is the sole repository inventory and runtime exposure ceiling? | The compiler-generated production capability contract. Static declarations own identity/governance; runtime code only binds behavior/state. Vite's resolved graph, lazy/virtual modules, and pinned sidecars form one canonical manifest. Strict registry/gateway boundaries reject unknown, raw, stale and mismatched provenance. CLI scenarios, heuristic extraction, depths, scopes, coverage joins, allowlists and runtime baselines are removed. `check` separates current-source integrity from snapshot/base PR drift | Continue improving heuristic extraction and scenario coverage; preserve raw registration as a supported escape hatch; claim only current mounted reach | Dynamic capability identity and unpinned runtime-loaded tools are unsupported. Public APIs break pre-1.0. Runtime behavior tests no longer double as inventory | S | [Architecture](../02-architecture.md#compiler-generated-contract-d40), [CLI](../20-cli.md), [Issue #45](https://github.com/Wiseair-srl/agent-surface/issues/45) |
| D41 | Is the D40 ceiling conventional or technically mandatory? | Mandatory `CapabilityAuthority`: verified/frozen manifest, nominal runtime instance, private WeakMap proof, semantic registration comparison, contract-only React/oRPC APIs, registry-owned adapter execution and authority-gated external tools. Raw definitions are inert. Repository-only test bypass is not exported or published | Keep the manifest optional; retain public raw/granular hooks; trust adapter curation callbacks not to replace execution | Same-realm hostile hosts can execute code outside the library and remain out of scope. Format v4 and public pre-1.0 APIs break | S | [Architecture](../02-architecture.md#mandatory-runtime-authority-d41) |


## Part B — Genuinely open questions

Each: context → options → current leaning → what it blocks.

**OQ-1 · `orpc-agent` manifest derivation.** The bridge needs `{path → schemas, effect, requiresApproval}` ([oRPC integration](../05-orpc-integration.md#identifying-procedures)); the `sideEffect → effect` mapping is settled there. Per [orpc-agent.dev](https://orpc-agent.dev), two concrete sources exist: a build-time export of the registry inventory (`capabilities.capabilities()`), or a bootstrap fetch of `runtime.describe(surface, { actor, context })` — per-user, so server visibility policies already apply to what the frontend may reference. *Leaning:* `describe()`-based fetch, hand-written manifest as escape hatch. *Still open:* whether orpc-agent grows a dedicated frontend/contextual exposure surface alongside `aiSdk`/`mcp`/`direct`, which would make this a first-class export. *Blocks:* `@agent-surface/orpc` DX polish (M9), not v0.1. **Decide before M9.**

**OQ-2 · WebMCP drift.** `navigator.modelContext` shape, permissions, and lifecycle are unstable. *Leaning:* adapter tracks whatever ships; no core concession ever. *Blocks:* only `@agent-surface/webmcp` (v0.3).

**OQ-3 · Cross-tab / multi-window surfaces.** One agent, several tabs: merge surfaces? namespace by window? *Options:* SharedWorker aggregator; BroadcastChannel federation; explicit multi-registry consumer. *Leaning:* explicit consumer-side federation (no magic in core). *Blocks:* nothing pre-1.0.

**OQ-4 · Relevance ranking / budgets beyond `priority`.** Automatic "what matters now" filtering needs real usage data. *Leaning:* keep manual `scope`/`priority`; measure catalogs from the example app first. *Blocks:* large-app ergonomics.

**OQ-5 · Contextual gating for server-side agents.** Frontend `when`/bindings influencing a *server-side* agent's tool availability requires a session-sync protocol (frontend context → orpc-agent). Security-sensitive: the server must treat pushed context as hints, never authority. *Leaning:* design doc first; nothing in 0.x. *Blocks:* the server-agent topology only.

**OQ-6 · Deep binding paths.** `bind` covers top-level fields; nested (`filters.city`) needs path syntax + schema surgery on nested objects. *Leaning:* wait for a real case; workaround = restructure procedure input. *Blocks:* some binding shapes.

**OQ-7 · i18n of descriptions/reasons.** Agent-facing strings are English-authored today; user-facing confirmation summaries may need the app's locale. *Leaning:* summaries composable by host (`requireConfirmation({summary})`) is enough for v0.1; full i18n unscoped.

**OQ-8 · Volatile snapshot hints.** A capped, non-versioned `snapshotHint` ("42 rows, 3 selected") would help discovery without observation round-trips, but breaks "snapshot = pure catalog" and invites state leakage. *Leaning:* keep out; revisit with OQ-4 evidence.

**OQ-9 · Meta-tools default threshold.** When should the toolset auto-switch `direct → meta`? D29 added a *documented* selection guide ([Adapters §choosing-a-mode](../09-adapters.md#choosing-a-mode)): direct under ~100 capabilities per route, scoped direct to ~200, meta beyond. That is advice, not a switch. *Leaning:* still never automatic in 0.x — the host knows its model's selection accuracy and its cache economics, and the library does not.

**OQ-10 · Streaming/subscribable observations.** Agents polling `readState` during long operations is clumsy; a subscription channel conflicts with "minimal transfer" and most tool protocols. *Leaning:* out of 0.x; `surfaceChanged` hint + re-read covers the common case.

**OQ-11 · Dedicated `@agent-surface/zod` sugar package.** One line saved per schema vs. another package to version. *Leaning:* skip until D19 subset friction is measured; the `zs()` helper in userland is 3 lines.

**OQ-12 · Confirmation UX for `view:` actions.** `confirmation:"required"` on a local action (clear draft) uses the same evidence protocol as domain calls — correct but possibly heavy. *Leaning:* keep uniform (one protocol, one audit trail); revisit if UX friction shows up in the example app.

**OQ-13 · Superseded by D40.** Granular runtime capability construction is unsupported by the compiler; there is no unresolved extraction state.

**OQ-14 · Superseded by D40.** Strict runtime registration rejects undeclared capabilities; there is no non-gating unknown bucket.
